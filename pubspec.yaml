name: prosecat
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.4+1

environment:
  sdk: '>=3.0.1 <4.0.0'  # Asegúrate de que el SDK de Dart sea compatible
  flutter: ">=3.19.0 <4.0.0"  # Actualiza a Flutter 3.29.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6  # Actualizado a la última versión
  google_maps_flutter: ^2.2.3  # Actualizado a la última versión
  flutter_speed_dial: ^7.0.0
  flutter_svg: ^2.0.7  # Actualizado a la última versión
  date_field: ^6.0.3
  provider: ^6.1.1  # Actualizado a la última versión
  http: ^1.1.0  # Actualizado a la última versión
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.2.2  # Actualizado a la última versión
  firebase_messaging: ^15.2.4  # Actualizado a la última versión
  just_audio: ^0.9.46  # Actualizado a la última versión
  firebase_core: ^3.12.1  # Actualizado a la última versión
  flutter_cache_manager: ^3.4.1  # Actualizado a la última versión
  google_maps_cluster_manager: ^3.1.0
  rflutter_alert: ^2.0.7  # Actualizado a la última versión
  fluttertoast: ^8.2.12  # Actualizado a la última versión
  share_plus: ^10.1.4
  skeletons: ^0.0.3  # Actualizado a la última versión
  url_launcher: ^6.2.0  # Actualizado a la última versión
  lottie: ^3.3.1  # Actualizado a la última versión
  custom_info_window: ^1.0.1
  flutter_spinkit: ^5.2.1
  syncfusion_flutter_datepicker: ^28.2.7
  convex_bottom_bar: ^3.2.0
  just_the_tooltip: ^0.0.12
  intl: any  # Actualizado a la última versión
  collection: ^1.17.1
  expandable_text: ^2.3.0
  path_provider: ^2.1.1  # Actualizado a la última versión
  permission_handler: ^11.4.0
  flutter_cached_pdfview: ^0.4.3  # Actualizado a la última versión
  device_info_plus: ^11.3.2
  dio: ^5.8.0+1
  animate_do: ^4.2.0
  gap: ^3.0.1
  chewie: ^1.10.0
  flutter_local_notifications: ^18.0.1
  flutter_tts: ^4.2.2  # Actualizado a la última versión
  cached_network_image: ^3.2.0
  flutter_vlc_player: ^7.3.1
  flutter_plugin_android_lifecycle: 2.0.27

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  localization: ^2.1.1
  flutter_launcher_icons: ^0.14.3
  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/icon/icon.png"
  adaptive_icon_background: "assets/icon/icon.png"
  min_sdk_android: 21

# The "flutter_lints" package below contains a set of recommended lints to
# encourage good coding practices. The lint set provided by the package is
# activated in the `analysis_options.yaml` file located at the root of your
# package. See that file for information about deactivating specific lint
# rules and activating additional ones.
flutter_lints: ^2.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - lib/l10n/
    - assets/images/
    - assets/icon/
    - assets/icon/flechas/
    - assets/icon/sensors/on/
    - assets/icon/sensors/off/
    - assets/icon/sensors/info/
    - assets/icon/status/
    - assets/sounds/

  fonts:
    - family: Roboto
      fonts:
        - asset: fonts/roboto/Roboto-Regular.ttf
        - asset: fonts/roboto/Roboto-Light.ttf
        - asset: fonts/roboto/Roboto-Bold.ttf

flutter_intl:
  enabled: true